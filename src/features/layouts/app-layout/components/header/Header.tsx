import React from "react";

const Header = ({ IsActiveMennu }: { IsActiveMennu: boolean }) => (
  <div
    className="bg-main-bg dark:bg-main-dark-bg navbar fixed
       md:static "
  >
    Header
  </div>
);

export default Header;
